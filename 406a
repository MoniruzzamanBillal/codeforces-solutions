#include <bits/stdc++.h>
#define pb push_back
using namespace std;

int main()
{
    int n;
    int a;
    cin >> n;
    int i;
    vector<int> s;

    for (i = 1; i <= n; i++)
    {
        cin >> a;
        s.pb(a);
    }

    sort(s.begin(), s.end());

    for (i = 0; i < n; i++)
    {
        cout << s[i] << " ";
    }

    return 0;
}
